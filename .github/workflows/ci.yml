name: CI

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  test:
    uses: PerlToolsTeam/github_workflows/.github/workflows/cpan-test.yml@main
    with:
      # Start conservative - Linux only since this is an XS module with C code
      # Can expand to macOS/Windows after confirming builds work
      os: '["ubuntu-latest"]'
      # Test a range of Perl versions
      perl_version: '["5.24", "5.30", "5.36", "5.40"]'

  coverage:
    uses: PerlToolsTeam/github_workflows/.github/workflows/cpan-coverage.yml@main

  perlcritic:
    uses: PerlToolsTeam/github_workflows/.github/workflows/cpan-perlcritic.yml@main
    with:
      level: 5
